# Disabled Mods Ledger

This ledger records mods intentionally kept out of active gameplay for the `20260701-055459` release.

## Disabled For Stability

- `immersive_portals-6.0.7-all.jar`
  - Reason: cross-dimensional rendering/entity sync risk in this large public-server build.
  - Status: not active in client or server `mods` folders.

- `portalgun-6.1.3+1.21.1.jar`
  - Reason: depends on the portal stack and inherits the same compatibility risk.
  - Status: not active in client or server `mods` folders.

## Verification

Active portal/portal-gun jar count across client and server: `0`.
